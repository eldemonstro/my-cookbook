<h1><%= @recipe_type.name %></h1>

<% if @recipes.any? %>
<%= render partial: 'shared/recipe_details'%>
<% else %>
  <p>Nenhuma receita encontrada para este tipo de receitas</p>
<% end %>

<%= link_to 'Editar tipo de receita', edit_recipe_type_path(@recipe_type) %>
